pipeline:
  build:
    image: golang
    commands:
      - go get
      - go build
      - go test

image: amd64/python
script:
  - touch /tmp/test.txt
  
  build:
    …

  notify:
    image: plugins/slack
    when:
      status: [ success, failure ]
  notify:
    image: plugins/slack
    channel: mywork
    username: nagaraju
